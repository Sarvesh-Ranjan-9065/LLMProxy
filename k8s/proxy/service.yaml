apiVersion: v1
kind: Service
metadata:
  name: llmproxy-gateway-service
spec:
  type: LoadBalancer
  selector:
    app: llmproxy-gateway
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP